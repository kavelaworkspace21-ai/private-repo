fastapi>=0.137
uvicorn[standard]>=0.49
python-multipart>=0.0.32
sqlalchemy>=2.0
alembic>=1.18
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-jose[cryptography]>=3.5
pyotp>=2.10
qrcode[pil]>=8.2
python-dotenv>=1.2
openai>=1.54
chromadb>=0.5.0
python-docx>=1.1
reportlab>=4.0
pdfplumber>=0.11
pypdf>=6.0                    # workbench upload PDF reader (app/services/workbench/uploads.py) — direct import, was undeclared
requests>=2.31               # eCourts/Indian Kanoon HTTP client — direct import, was only present transitively
httpx>=0.27
apscheduler>=3.10
psycopg[binary]>=3.2          # PostgreSQL/Aurora driver (production DB)
cryptography>=43              # field encryption at rest (app/db/crypto.py)

# ── Security floors on transitive deps (S0.3, 2026-07-21 pip-audit) ──────────────
# Pulled in transitively (Pillow via qrcode[pil]/reportlab/pdfplumber; pydantic-settings
# via fastapi/chromadb) but pinned here to their patched versions so the blocking CI audit
# stays green. Bump these as new advisories land.
pillow>=12.3.0                # fixes 20 CVEs in <=12.2.0 (PYSEC-2026-2253.. / CVE-2026-59197..)
pydantic-settings>=2.14.2    # fixes GHSA-4xgf-cpjx-pc3j
